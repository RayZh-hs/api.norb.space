# api.norb.space

This repository provides API support for `norb.space` remote development.

The API endpoint is used for providing a unified backend api service for multiple apps in the `norb.space` domain, and includes builtin support for updating and managing remote development environments.

For detailed documentation, please refer to the [Docs](docs/) directory.
